'use strict';

/**
 * meeting-agenda controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::meeting-agenda.meeting-agenda');
